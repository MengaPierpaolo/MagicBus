@model TDiary.Providers.ViewModel.Model.SightViewModel
@{ Layout = "~/Views/Shared/_EditorLayout.cshtml"; }

<div class="content">
    <form class="form" role="form" asp-action="Add">
        @{
            Html.RenderPartial("_SightInfo", Model);
        }
    </form>
</div>