@model TDiary.Providers.ViewModel.Model.HomeViewModel

<div id="myCarousel" class="carousel journey-carousel">
    <ol class="carousel-indicators">
        @for (int i = 0; i < Model.Journeys.Count(); i++)
        {
            if (i == 0)
            {
                <li data-target="#myCarousel" data-slide-to="0" class="active"></li>
            }
            else
            {
                <li data-target="#myCarousel" data-slide-to="@i"></li>
            }
        }
    </ol>
    <div class="carousel-inner journey-carousel" role="listbox">
        @for (int i = 0; i < Model.Journeys.Count(); i++)
        {
            if (i == 0)
            {
                <div class="item active">
                    <div class="carousel-caption journey" role="option">
                        <p>
                            @Model.Journeys.ToList()[i].Name
                        </p>
                    </div>
                </div>
            }
            else
            {
                <div class="item">
                    <div class="carousel-caption journey" role="option">
                        <p>
                            @Model.Journeys.ToList()[i].Name
                        </p>
                    </div>
                </div>
            }
        }
    </div>
    <a class="left carousel-control" href="#myCarousel" role="button" data-slide="prev">
        <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
        <span class="sr-only">Previous</span>
    </a>
    <a class="right carousel-control" href="#myCarousel" role="button" data-slide="next">
        <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
        <span class="sr-only">Next</span>
    </a>
</div>
